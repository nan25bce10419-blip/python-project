# python-project
a simple python project that implements a basic calculator using user defined functions. The calculator performs addition , subtraction , multiplication and division also handles invalid inputs safely.
